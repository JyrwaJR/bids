import React from 'react';

type Props = {
  open: boolean;
  onClose: () => void;
};
const AddCentreProject = ({ onClose, open }: Props) => {
  return <div>AddCentreProject</div>;
};

export default AddCentreProject;
