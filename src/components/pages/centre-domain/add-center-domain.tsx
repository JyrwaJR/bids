import React from 'react';

type Props = {
  open: boolean;
  onClose: () => void;
};
const AddCentreDomain = ({ onClose, open }: Props) => {
  return <div>AddCentreDomain</div>;
};

export default AddCentreDomain;
